package main

func main() {
	/*a:=5

	switch a{
		case 1:
			fmt.Println("one")
		case 2:
			fmt.Println("two")
		default:
			fmt.Println("default")
	}
	*/

	//fallthrough
	/*
	a := -2

	switch {
	case a < -1:
		fmt.Println("a is less than -1")
		fallthrough
	case a < 0:
		fmt.Println("a is less than 0")
		fallthrough
	case a < 1:
		fmt.Println("a is less than 1")
	default:
		fmt.Println("default")
	}
	*/


}
